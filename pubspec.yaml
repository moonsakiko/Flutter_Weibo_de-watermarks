name: weibo_cleaner
description: Weibo Watermark Remover
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.7
  file_picker: ^6.1.1
  permission_handler: ^11.3.0
  fluttertoast: ^8.2.4
  path_provider: ^2.1.2
  # 👇 网络请求新增
  http: ^1.2.0
  dio: ^5.4.0 
  # 👇 简单的本地存储用来存配置
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
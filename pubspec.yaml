name: weibo_cleaner
description: Weibo Watermark Remover
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.7
  file_picker: ^6.1.1
  permission_handler: ^11.3.0
  fluttertoast: ^8.2.4
  path_provider: ^2.1.2
  dio: ^5.4.0
  # 👇 纯净的 HTTP 库，有时候比 Dio 处理重定向更直观
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/

flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/images/icon.png"
  min_sdk_android: 24